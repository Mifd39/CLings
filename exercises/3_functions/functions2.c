// Learning Goal: Define a function with arguments and a return value.
//
// Functions can accept input values (arguments) and return a result.
//
// Example:
// int square(int num) {
//     return num * num;
// }

#include <stdio.h>

// I AM NOT DONE

// TODO: Define a function named 'add' that takes two integers as arguments and returns their sum.
// The prototype should look like: int add(int a, int b)


int main() {
    int sum = add(5, 10);
    printf("The sum is %d\n", sum);
    return 0;
}
