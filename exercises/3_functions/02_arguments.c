// Learning Goal: Define a function with arguments and a return value.
/*
    Context:
    Functions become much more powerful when they can accept input and provide output.
    Imagine a function as a machine: you feed raw materials (arguments) into a slot,
    the machine processes them, and it dispenses a finished product (return value).
*/
//
// Functions can accept input values (arguments) and return a result.
//
// Example:
// int square(int num) {
//     return num * num;
// }

#include <stdio.h>

// I AM NOT DONE

// TODO: Define a function named 'add' that calculates and returns the sum of two integers.


int main() {
    int sum = add(5, 10);
    printf("The sum is %d\n", sum);
    return 0;
}
