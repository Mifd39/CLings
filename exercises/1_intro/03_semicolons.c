// Learning Goal: Fix syntax errors.
/*
    Context:
    Human languages (like English) are flexible; you can make small grammar mistakes and still be understood.
    Computer languages (like C) are strict. They need exact instructions.
    One of the most common rules in C is that every instruction (statement) must end with a semicolon (;).
    It's like the period at the end of a sentence. Without it, the compiler gets confused.
*/
//
// C is a compiled language and it is very strict about syntax.
// Every statement in C must end with a semicolon (;).
// This tells the compiler where one instruction ends and the next one begins.

#include <stdio.h>

// I AM NOT DONE

int main() {
    // TODO: Fix the syntax error so the code compiles.
    printf("Hello World!\n")
    return 0;
}
