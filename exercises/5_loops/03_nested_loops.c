// Learning Goal: Use nested loops.
/*
    Context:
    What if you need to work with a grid, like a chessboard or a spreadsheet?
    You have rows and columns. To visit every cell, you need a loop within a loop.
    The outer loop handles the rows, and the inner loop handles the columns.
*/
//
// You can put a loop inside another loop (nested loops).
// This is often used for working with 2D grids or matrices.

#include <stdio.h>

// I AM NOT DONE

int main() {
    // TODO: Use nested loops to print a 3x3 square of asterisks (*).
    /*
        Expected Output:
        ***
        ***
        ***
    */

    return 0;
}
