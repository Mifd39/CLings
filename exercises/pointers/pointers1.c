// Learning Goal: Understand the basics of pointers and the address-of operator.
#include <stdio.h>

// I AM NOT DONE

int main() {
    int x = 42;
    // TODO: Create a pointer 'p' that points to 'x'

    printf("The value of x via pointer is %d\n", *p);
    return 0;
}
