// Learning Goal: Pass pointers to functions.
#include <stdio.h>

// I AM NOT DONE

// TODO: Define a function 'swap' that takes two int pointers and swaps the values they point to.


int main() {
    int a = 5, b = 10;
    printf("Before swap: a = %d, b = %d\n", a, b);

    // TODO: Call swap with the addresses of a and b

    printf("After swap: a = %d, b = %d\n", a, b);
    return 0;
}
