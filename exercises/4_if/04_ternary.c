// Learning Goal: Understand the ternary operator.
/*
    Context:
    The "ternary operator" is a shortcut for a simple if-else statement.
    It's called "ternary" because it takes three parts:
    1. A condition.
    2. A value if true.
    3. A value if false.
    Format: condition ? value_if_true : value_if_false;
    It's great for short assignments.
*/

#include <stdio.h>

// I AM NOT DONE

int main() {
    int a = 42;
    int b = 99;

    // TODO: Use the ternary operator to find the maximum of 'a' and 'b' and assign it to 'max'.
    // Logic: Is a > b? If yes, result is a. If no, result is b.
    int max = 0;

    printf("The max value is: %d\n", max);

    return 0;
}
