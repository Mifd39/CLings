// Learning Goal: Use the ternary operator for concise conditional logic.
/*
    Context:
    The ternary operator is a shorthand way to write simple if-else statements.
    It's like asking a quick question: "Is this true? If yes, do this; if no, do that."
    It allows you to assign a value based on a condition in a single line of code.
*/
//
// Syntax: condition ? value_if_true : value_if_false
// Example: int max = (a > b) ? a : b;

#include <stdio.h>

// I AM NOT DONE

int main() {
    int temperature = 25;
    const char* status;

    // TODO: Use the ternary operator to assign "Warm" to status if temperature > 20, else "Cold".
    // status = ...

    printf("It is %s outside.\n", status);
    return 0;
}
