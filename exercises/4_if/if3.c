// Learning Goal: Use logical operators && and ||.
#include <stdio.h>

// I AM NOT DONE

int main() {
    int age = 25;
    int has_license = 1;

    // TODO: Check if age is greater than or equal to 18 AND has_license is true (1)
    if ( ) {
        printf("You can drive!\n");
    } else {
        printf("You cannot drive.\n");
    }

    return 0;
}
