// Learning Goal: Learn how to declare and initialize integer variables.
/*
    Context:
    Imagine you have a box where you can store a single item.
    In programming, we call these boxes "variables". You can put a value in, take it out,
    or replace it with a new one.
    To use a box, you first need to label it (give it a name) and decide what kind of things it can hold (its type).
    Let's create a variable to hold an integer (a whole number).
*/
//
// In C, variables must be declared with a type before they can be used.
// The syntax is: type variable_name = value;
//
// 'int' is the type for integer numbers (whole numbers).

#include <stdio.h>

// I AM NOT DONE

int main() {
    // TODO: Declare and initialize a variable 'x' with the value 5 so the print statement works.


    printf("x is %d\n", x);
    return 0;
}
